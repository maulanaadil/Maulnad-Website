import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const BioYear = styled(Box)`
  font-weight: bold;
  margin-right: 1em;
`

export const BioDesc = styled(Box)`
  font-weight: normal;
  margin-bottom: 1em;
`
